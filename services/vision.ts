// Placeholder: Vision API for macro detection.
// TODO: implement image -> macros detection (calories, protein, carbs, fat).

export async function detectMacros(_imageUri: string) {
  // TODO: call the vision API and return detected macros.
  return null;
}
