// Re-export of the canonical token set so older imports keep working.
// Source of truth is theme.ts and data.ts.
export { colors as uiColors, macroColors, colors } from './theme';
export { MOODS, MOOD_ORDER } from './data';
