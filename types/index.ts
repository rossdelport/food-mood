// Placeholder: shared TypeScript types for the app.

export type Mood = {
  id: string;
  color: string;
  label: string;
};

export type Meal = {
  id: string;
  name: string;
  createdAt: string;
};

export type JournalEntry = {
  id: string;
  text: string;
  createdAt: string;
};
